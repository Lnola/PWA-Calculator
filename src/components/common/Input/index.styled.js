import styled from 'styled-components';

export const StyledInput = styled.textarea`
  padding: 5%;
  height: 100%;
  border: none;
  font-size: 2rem;
  font-family: inherit;

  &:focus {
    outline: none;
  }
`;
